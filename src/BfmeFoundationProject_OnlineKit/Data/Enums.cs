namespace BfmeFoundationProject.OnlineKit.Data
{
    public enum OnlineMenuVisualState
    {
        DesignerMode,
        Unloaded,
        CheckingForUpdates,
        Loading,
        DownloadProgress,
        UpdateProgress,
        RepairProgress,
        Loaded,
        ServerDown
    }
}
