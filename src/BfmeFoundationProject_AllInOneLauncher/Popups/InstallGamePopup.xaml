<generic:PopupBody x:Class="BfmeFoundationProject.AllInOneLauncher.Popups.InstallGamePopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BfmeFoundationProject.AllInOneLauncher.Popups"
             xmlns:elements="clr-namespace:BfmeFoundationProject.AllInOneLauncher.Elements"
             xmlns:converter="clr-namespace:BfmeFoundationProject.AllInOneLauncher.Elements.Converter"
             xmlns:system="clr-namespace:System;assembly=netstandard"
             xmlns:generic="clr-namespace:BfmeFoundationProject.AllInOneLauncher.Elements.Generic"
             mc:Ignorable="d" 
             Width="700" HorizontalAlignment="Center" VerticalAlignment="Center">

    <generic:PopupBody.Resources>
        <converter:BooleanToVisibilityConverter x:Key="BooleanToVisibility"></converter:BooleanToVisibilityConverter>
    </generic:PopupBody.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Name="TextTitleInstall" Grid.Row="0" Text="{DynamicResource InstallGamePopupTitle}" Foreground="White" FontSize="20" FontWeight="Medium" HorizontalAlignment="Left" VerticalAlignment="Center" />
        <Rectangle Grid.Row="1" Fill="White" Opacity="0.2" Margin="0,10,0,0" VerticalAlignment="Top" Height="1"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="/Resources/Images/BFME1LanguageSupport.png" Stretch="None" Width="450" Height="270">
                    <Image.OpacityMask>
                        <VisualBrush>
                            <VisualBrush.Visual>
                                <Border Background="Black" SnapsToDevicePixels="True" CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=Border}}" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}" Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Image.OpacityMask>
                </Image>
            </Grid>
            <Grid Grid.Column="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" FontSize="14" FontWeight="SemiBold" Text="{DynamicResource InstallGamePopupLanguage}" Foreground="white"/>
                    <elements:DropdownPicker Grid.Row="2" x:Name="LanguageDropdown" VerticalAlignment="Center" FontSize="20" Height="40" ColorStyle="Navy" Corners="4">
                        <elements:DropdownPicker.Options>
                            <system:String>English</system:String>
                            <system:String>French</system:String>
                            <system:String>German</system:String>
                            <system:String>Italian</system:String>
                            <system:String>Spanish</system:String>
                            <system:String>Swedish</system:String>
                            <system:String>Dutch</system:String>
                            <system:String>Polish</system:String>
                            <system:String>Norwegian</system:String>
                        </elements:DropdownPicker.Options>
                    </elements:DropdownPicker>
                    <TextBlock Grid.Row="4" FontSize="14" FontWeight="SemiBold" Text="{DynamicResource InstallGamePopupLocation}" Foreground="white"/>

                    <TextBlock Grid.Row="6" x:Name="SelectLocationError" FontSize="14" FontWeight="SemiBold" Padding="15,0" Text="{DynamicResource InstallGamePopupNoDrive}" Foreground="red" Visibility="Collapsed" />
                    <Grid Grid.Row="6" x:Name="SelectLocationArea" Visibility="Visible">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,6,0,6">
                            <ToggleButton x:Name="SelectLocationAdvancedToggle" Width="60" Height="28" VerticalAlignment="Center">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Focusable" Value="False" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Grid Width="60" Height="28">
                                                        <Border x:Name="Track" CornerRadius="14" Background="#444" />
                                                        <Ellipse x:Name="Thumb" Width="22" Height="22" Fill="White" Margin="3" HorizontalAlignment="Left">
                                                            <Ellipse.RenderTransform>
                                                                <TranslateTransform />
                                                            </Ellipse.RenderTransform>
                                                        </Ellipse>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Track" Property="Background" Value="#4CAF50" />
                                                            <!-- Move the thumb to the right by adjusting margin instead of using RenderTransform path -->
                                                            <Setter TargetName="Thumb" Property="Margin" Value="35,0,0,0" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <TextBlock Text="{DynamicResource InstallGamePopupLocationAdvanced}" Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2">
                            <generic:SmoothScrollViewer x:Name="SelectLocationList" Visibility="{Binding IsChecked, ElementName=SelectLocationAdvancedToggle, Converter={StaticResource BooleanToVisibility}, ConverterParameter='invert'}">
                                <StackPanel x:Name="locations" />
                            </generic:SmoothScrollViewer>
                            <Button x:Name="SelectFolderButton" Click="OnSelectFolderClicked" HorizontalAlignment="Stretch" VerticalAlignment="Top" HorizontalContentAlignment="Left" Padding="15" Visibility="{Binding IsChecked, ElementName=SelectLocationAdvancedToggle, Converter={StaticResource BooleanToVisibility}}">
                                <StackPanel>
                                    <TextBlock x:Name="SelectedPathText" Text="" Foreground="White" FontSize="12" TextWrapping="Wrap"/>
                                    <TextBlock x:Name="SelectedFreeSpaceText" Text="" Foreground="White" FontSize="11" Opacity="0.8"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Grid.Column="0" Grid.Row="2" x:Name="ButtonAccept" Style="{StaticResource HiglightButton}" VerticalAlignment="Center" Click="OnInstallClicked" Width="150" Margin="0,0,10,0" Content="{DynamicResource GenericInstall}" IsEnabled="false" />
            <Button Grid.Column="1" Grid.Row="2" x:Name="ButtonCancel" Content="{DynamicResource GenericCancel}" VerticalAlignment="Center" Click="OnCancelClicked" Width="150"/>
        </StackPanel>
    </Grid>
</generic:PopupBody>
